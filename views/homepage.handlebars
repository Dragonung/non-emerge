<!-- Loop over each object in the jots array -->
{{#each jots as |jot| }}
<div class="row mb-4 jot">
  <div class="col-md-5">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      {{{get_emoji}}}
      <a href="/jot/{{jot.id}}">{{jot.driver}}</a>
    </h2>
    <p>
      <span for="img" aria-label="money">💲</span>
      <!-- Pass needed_funding value to the helper function -->
      
     
    </p>
    <p>Created by {{jot.user.name}} on </p>
  </div>
  <div class="col-md-7">
    <p>
      {{jot.description}}
    </p>
  </div>
</div>
{{/each}}
